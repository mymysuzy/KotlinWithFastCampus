add view -> 실전 프로젝트에선 잘 사용되지 않는 기능

- 부모뷰가 될 수 있는 뷰에 자식뷰들을 "코드상"에서 "동적으로" 붙여 넣는다
- 자식뷰의 형태(xml배치나 속성)는 동일하지만 그 내용물만 다르다
- 자주 볼 수 있는 형태로는 ListView가 있다

add view -> 원리
ListView, recyclerView -> addView기반 구현
