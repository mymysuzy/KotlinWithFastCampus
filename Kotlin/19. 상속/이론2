오버라이딩(Overriding) -> 반드시 알아야 하는 기능.
- 부모클래스가 사용하고 있는 멤버와 동일한 이름의 멤버를 만들 수 없다.
    * 오버라이딩을 통해 부모클래스가 가지고있는 함수를 재정의 할 수 있다.
    * 재정의 할 때는 함수 맨 앞에 override키워드를 적어준다.

- 재정의
    * 이전것을 덮어쓴다




오버로딩(Overloading) -> 거의 사용 X
- 동일한 이름의 함수를 복수개 만드는 것은 불가능
    * 파라미터를 바꿔주자.
