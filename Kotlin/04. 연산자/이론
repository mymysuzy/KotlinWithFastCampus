연산자
    1. 산술연산자
        - 사칙연산, %
            * % : 나머지를 반환.
    2. 대입연산자
    3. 복합대입연산자
        - +=, -=, &= 등
    4. 증감연산자
        - ++, --
    5. 비교연산자
        - ==, != 등
            * === : 객체가 같은가? -> 잘 사용하지 않는다.
            * !== : 객체가 다른가? -> 잘 사용하지 않는다.

        - ==, != : `값 자체`가 같은지 물어보는 연산자
        - ===, !== : `주소값`이 같은지 물어보는 연산자

    6. 논리연산자
        - &&, ||. !
            * && : and
            * || : or
            * ! : 참거짓 바꾸기